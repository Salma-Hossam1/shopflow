PORT=3001

DB_HOST=auth-db
DB_PORT=5432
DB_NAME=authdb
DB_USER=authuser
DB_PASSWORD=authpassword

# Never commit the real value — this is just the variable name as a reminder
JWT_SECRET=change_this_to_a_long_random_string