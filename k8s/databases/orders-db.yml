apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-db-deployment
  namespace: shopflow-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders-db        
  template:
    metadata:
      labels:
        app: orders-db     
    spec:
      nodeSelector:
        shopflow/role: storage      
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom:
            - secretRef:
                name: orders-db-secret  
          resources:
            requests:
              memory: 64Mi          
              cpu: 50m               
            limits:
              memory: 256Mi
              cpu: 150m
          volumeMounts:
            - name: orders-db-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: orders-db-storage
          persistentVolumeClaim:
            claimName: orders-pvc   

---
apiVersion: v1
kind: Service
metadata:
  name: orders-db
  namespace: shopflow-dev
spec:
  type: ClusterIP
  selector:
    app: orders-db
  ports:
    - port: 5432
      targetPort: 5432